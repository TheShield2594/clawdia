<%# Overview opens on a greeting rather than a .panel-head paragraph, so it
    declares the description that docs-panels.js puts in docs/FEATURES.md here.
    summary: Server stats, the getting-started checklist, and quick settings. %>
        <section id="overview" class="panel active" style="display:block;">

            <div class="dash-header">
                <div>
                    <h1 class="dash-greeting">Good to see you, <em><%= user.username %>.</em></h1>
                    <div class="dash-subgreeting">Clawdia is running — managing <b style="color:var(--cream-50);"><%= guild.name %></b>.</div>
                </div>
            </div>

            <%# Every module card and checklist step reports its state twice: in the
                `on` class the toggle pip is painted from, and in the text a screen
                reader is read (#910). Both read the one variable below, because a
                card whose colour and whose words disagree would be worse than the
                colour-only card this replaced. %>
            <%
            var stepDone = {
                welcome:    !!(settings.welcome && settings.welcome.enabled && settings.welcome.channelId),
                moderation: !!(settings.moderation && settings.moderation.enabled && settings.moderation.logChannelId),
                leveling:   !!(settings.leveling && settings.leveling.enabled),
                economy:    !!(settings.economy && settings.economy.enabled),
                ai:         !!(settings.ai && settings.ai.enabled),
            };
            var moduleOn = {
                welcome:    !!(settings.welcome && settings.welcome.enabled),
                moderation: !!(settings.moderation && settings.moderation.enabled),
                leveling:   !!(settings.leveling && settings.leveling.enabled),
                economy:    !!(settings.economy && settings.economy.enabled),
                ai:         !!(settings.ai && settings.ai.enabled),
                rss:        !!(settings.rss?.enabled || settings.rssFeeds?.length > 0
                                || settings.dailyNews?.enabled || settings.dailyNewsProfiles?.length > 0),
            };
            var setupDone = Object.values(stepDone).every(Boolean);
            %>

            <!-- Getting started checklist -->
            <div id="getting-started-wrap" style="margin-bottom:1.5rem;<%= setupDone ? 'display:none' : '' %>">
                <div class="dash-card" id="getting-started-card">
                    <%# The collapse is a real button, not a div with a click handler
                        (#882): a keyboard user could not reach the old one at all, and
                        the only sign it was expanded was the ▾/▸ glyph, which a screen
                        reader has no reason to read out. The glyph is decorative now
                        and aria-expanded carries the state. The button stretches across
                        the header so the click target stays the whole row it was. %>
                    <div class="dash-card-header gs-header">
                        <h2 class="dash-card-title">
                            <button type="button" id="gs-toggle" class="gs-toggle"
                                    aria-expanded="true" aria-controls="getting-started-body"
                                    data-action="toggle-getting-started">
                                <%# The subtitle sits inside the button, not beside it: the whole
                                    header used to be the click target and a subtitle left outside
                                    would be the one part of it that stopped working. It joins the
                                    button's accessible name, which reads well for a disclosure —
                                    "Getting started, 2 of 5 steps complete". %>
                                <span class="gs-toggle-text">
                                    <span>Getting started</span>
                                    <span class="dash-card-sub" id="gs-subtitle">Complete these steps to finish setting up Clawdia</span>
                                </span>
                                <span id="gs-toggle-icon" class="gs-toggle-icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></span>
                            </button>
                        </h2>
                    </div>
                    <div id="getting-started-body">
                        <div class="dash-module-list" style="display:flex;flex-direction:column;gap:.35rem;padding-top:.25rem">
                            <button type="button" class="dash-module gs-step <%= stepDone.welcome ? 'on' : '' %>" data-gs-step="welcome" data-action="goto-tab" data-tab="welcome">
                                <div class="dash-module-icon"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M3 16c2 0 2-3 4-3s2 3 4 3 2-3 4-3 2 3 4 3"/><path d="M3 11c2 0 2-3 4-3s2 3 4 3 2-3 4-3 2 3 4 3"/></svg></div>
                                <div class="dash-module-body"><h4>Set up a welcome message</h4><p>Greet new members automatically</p></div>
                                <div class="dash-module-toggle" aria-hidden="true"></div>
                                <span class="sr-only"><%= stepDone.welcome ? '— done' : '— not done yet' %></span>
                            </button>
                            <button type="button" class="dash-module gs-step <%= stepDone.moderation ? 'on' : '' %>" data-gs-step="moderation" data-action="goto-tab" data-tab="moderation">
                                <div class="dash-module-icon"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3l7 2.6v5.4c0 4.5-3 7.5-7 8.5-4-1-7-4-7-8.5V5.6L12 3z"/><path d="M9.1 13.7l2.4-2.9M10.8 14.7l2.4-2.9M12.5 15.6l2.4-2.9"/></svg></div>
                                <div class="dash-module-body"><h4>Configure a moderation log channel</h4><p>Track all mod actions in one place</p></div>
                                <div class="dash-module-toggle" aria-hidden="true"></div>
                                <span class="sr-only"><%= stepDone.moderation ? '— done' : '— not done yet' %></span>
                            </button>
                            <button type="button" class="dash-module gs-step <%= stepDone.leveling ? 'on' : '' %>" data-gs-step="leveling" data-action="goto-tab" data-tab="leveling">
                                <div class="dash-module-icon"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="13" width="3.4" height="7" rx="1.2"/><rect x="10.3" y="9" width="3.4" height="11" rx="1.2"/><rect x="16.6" y="5" width="3.4" height="15" rx="1.2"/></svg></div>
                                <div class="dash-module-body"><h4>Enable leveling</h4><p>Reward active members with XP</p></div>
                                <div class="dash-module-toggle" aria-hidden="true"></div>
                                <span class="sr-only"><%= stepDone.leveling ? '— done' : '— not done yet' %></span>
                            </button>
                            <button type="button" class="dash-module gs-step <%= stepDone.economy ? 'on' : '' %>" data-gs-step="economy" data-action="goto-tab" data-tab="economy">
                                <div class="dash-module-icon"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="8"/><ellipse cx="12" cy="13.3" rx="2.2" ry="1.7" fill="currentColor" stroke="none"/><circle cx="9.3" cy="10.7" r="1" fill="currentColor" stroke="none"/><circle cx="12" cy="9.6" r="1" fill="currentColor" stroke="none"/><circle cx="14.7" cy="10.7" r="1" fill="currentColor" stroke="none"/></svg></div>
                                <div class="dash-module-body"><h4>Add your first shop item</h4><p>Start your server economy</p></div>
                                <div class="dash-module-toggle" aria-hidden="true"></div>
                                <span class="sr-only"><%= stepDone.economy ? '— done' : '— not done yet' %></span>
                            </button>
                            <button type="button" class="dash-module gs-step <%= stepDone.ai ? 'on' : '' %>" data-gs-step="ai" data-action="goto-tab" data-tab="ai">
                                <div class="dash-module-icon"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3l1.6 4.6L18 9l-4.4 1.4L12 15l-1.6-4.6L6 9l4.4-1.4z"/></svg></div>
                                <div class="dash-module-body"><h4>Connect an AI provider</h4><p>Enable conversational AI for your server</p></div>
                                <div class="dash-module-toggle" aria-hidden="true"></div>
                                <span class="sr-only"><%= stepDone.ai ? '— done' : '— not done yet' %></span>
                            </button>
                        </div>
                        <div style="text-align:right;margin-top:.75rem">
                            <button class="btn" type="button" data-action="dismiss-getting-started" style="font-size:.8rem;opacity:.6">Dismiss checklist</button>
                        </div>
                    </div>
                </div>
            </div>

            <%# KPI strip (v5). Members leads as the hero — it is the metric with
                trend data (memberGrowth off /stats), so it carries the delta and
                the only sparkline; the rest recede to a number and a foot. The
                five tiles that map to a panel are buttons that navigate there via
                the shared goto-tab action, so a headline is also a way in. Trend
                colour lives in the delta chip, not the number, and stays warm
                (moss up / terracotta down) — see styles.css. All styling is in
                classes: this block keeps the inline-style ratchet falling (#692). %>
            <!-- KPIs -->
            <div class="dash-kpis">
                <button type="button" class="dash-kpi dash-kpi-hero dash-kpi-link" id="kpi-members" data-action="goto-tab" data-tab="analytics">
                    <span class="dash-kpi-cta" aria-hidden="true"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
                    <div class="dash-kpi-label">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="9" cy="8" r="3.5"/><path d="M2 20c0-3.5 3-6 7-6s7 2.5 7 6"/><circle cx="17" cy="9" r="2.5"/><path d="M22 19c0-2.5-2-4.5-5-4.5"/></svg>
                        Members
                    </div>
                    <div class="dash-kpi-value" id="kpi-members-value">—</div>
                    <div class="dash-kpi-meta">
                        <span class="dash-kpi-delta" id="kpi-members-delta" hidden></span>
                        <span class="dash-kpi-foot" id="kpi-members-foot">loading…</span>
                    </div>
                    <div class="dash-kpi-spark" id="kpi-members-spark" aria-hidden="true"></div>
                    <span class="sr-only">— open Analytics</span>
                </button>
                <button type="button" class="dash-kpi dash-kpi-link" id="kpi-moderation" data-action="goto-tab" data-tab="moderation">
                    <span class="dash-kpi-cta" aria-hidden="true"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
                    <div class="dash-kpi-label">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3l7 2.6v5.4c0 4.5-3 7.5-7 8.5-4-1-7-4-7-8.5V5.6L12 3z"/><path d="M9.1 13.7l2.4-2.9M10.8 14.7l2.4-2.9M12.5 15.6l2.4-2.9"/></svg>
                        Moderation
                    </div>
                    <div class="dash-kpi-value" id="kpi-mod-value">—</div>
                    <div class="dash-kpi-meta"><span class="dash-kpi-foot" id="kpi-mod-foot">loading…</span></div>
                    <span class="sr-only">— open Moderation</span>
                </button>
                <div class="dash-kpi">
                    <div class="dash-kpi-label">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/></svg>
                        Modules on
                    </div>
                    <%
                    var _moduleChecks = [
                        !!(settings.welcome?.enabled),
                        !!(settings.farewell?.enabled),
                        !!(settings.birthdays?.enabled),
                        !!(settings.moderation?.enabled),
                        !!(settings.leveling?.enabled),
                        !!(settings.economy?.enabled),
                        !!(settings.heist?.enabled),
                        !!(settings.achievements?.enabled),
                        !!(settings.raidDetection?.enabled),
                        !!(settings.antiNuke?.enabled),
                        !!(settings.starboard?.enabled),
                        !!(settings.eventLog?.enabled),
                        !!(settings.quests?.enabled),
                        !!(settings.newspaper?.enabled),
                        !!(settings.season?.enabled),
                        !!(settings.progressionTracks?.enabled),
                        !!(settings.suggestions?.enabled),
                        !!(settings.dailyNews?.enabled || settings.rss?.enabled || settings.rssFeeds?.length > 0 || settings.dailyNewsProfiles?.length > 0),
                        !!(settings.tempVoice?.enabled),
                        !!(settings.commandPolicies?.enabled),
                        !!(settings.ai?.enabled),
                        !!(settings.bibleVerse?.enabled),
                    ];
                    var moduleCount = _moduleChecks.filter(Boolean).length;
                    var totalModules = _moduleChecks.length;
                    %>
                    <div class="dash-kpi-value"><%= moduleCount %></div>
                    <div class="dash-kpi-meta"><span class="dash-kpi-foot">of <%= totalModules %> features on</span></div>
                    <%# A ratio, not a time series — so a progress meter, not a sparkline. %>
                    <div class="dash-kpi-meter" aria-hidden="true"><i style="width:<%= totalModules ? Math.round((moduleCount / totalModules) * 100) : 0 %>%"></i></div>
                </div>
                <div class="dash-kpi">
                    <div class="dash-kpi-label">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M13 2L4 14h6l-1 8 9-12h-6l1-8z"/></svg>
                        Bot Status
                    </div>
                    <div class="dash-kpi-value status"><span class="dash-kpi-dot" aria-hidden="true"></span><span id="kpi-bot-value">—</span></div>
                    <div class="dash-kpi-meta"><span class="dash-kpi-foot mono" id="kpi-bot-foot">checking…</span></div>
                </div>
                <button type="button" class="dash-kpi dash-kpi-link" id="kpi-economy-overview" data-action="goto-tab" data-tab="economy">
                    <span class="dash-kpi-cta" aria-hidden="true"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
                    <div class="dash-kpi-label">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="8"/><ellipse cx="12" cy="13.3" rx="2.2" ry="1.7" fill="currentColor" stroke="none"/><circle cx="9.3" cy="10.7" r="1" fill="currentColor" stroke="none"/><circle cx="12" cy="9.6" r="1" fill="currentColor" stroke="none"/><circle cx="14.7" cy="10.7" r="1" fill="currentColor" stroke="none"/></svg>
                        Economy
                    </div>
                    <div class="dash-kpi-value" id="kpi-eco-value">—</div>
                    <div class="dash-kpi-meta"><span class="dash-kpi-foot" id="kpi-eco-foot">active users (7d)</span></div>
                    <span class="sr-only">— open Economy</span>
                </button>
                <button type="button" class="dash-kpi dash-kpi-link" id="kpi-leveling-overview" data-action="goto-tab" data-tab="leveling">
                    <span class="dash-kpi-cta" aria-hidden="true"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
                    <div class="dash-kpi-label">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 18l5-5 4 4 5-7 4 3"/></svg>
                        Leveling
                    </div>
                    <div class="dash-kpi-value" id="kpi-level-value">—</div>
                    <div class="dash-kpi-meta"><span class="dash-kpi-foot" id="kpi-level-foot">top level</span></div>
                    <span class="sr-only">— open Leveling</span>
                </button>
                <button type="button" class="dash-kpi dash-kpi-link" id="kpi-ai-overview" data-action="goto-tab" data-tab="ai">
                    <span class="dash-kpi-cta" aria-hidden="true"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
                    <div class="dash-kpi-label">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7.4 7.3L6.1 4l3.3 1.8M16.6 7.3L17.9 4l-3.3 1.8"/><path d="M5 7.2h14a1.2 1.2 0 0 1 1.2 1.2v6.3a1.2 1.2 0 0 1-1.2 1.2h-6.3L9 20v-3.1H5a1.2 1.2 0 0 1-1.2-1.2V8.4A1.2 1.2 0 0 1 5 7.2z"/><circle cx="9.7" cy="11.6" r=".95" fill="currentColor" stroke="none"/><circle cx="14.3" cy="11.6" r=".95" fill="currentColor" stroke="none"/></svg>
                        Ask Clawdia
                    </div>
                    <div class="dash-kpi-value" id="kpi-ai-value">—</div>
                    <div class="dash-kpi-meta"><span class="dash-kpi-foot" id="kpi-ai-foot">chats (total)</span></div>
                    <span class="sr-only">— open Ask Clawdia</span>
                </button>
            </div>

            <!-- Modules grid + bot card -->
            <div class="dash-two-col">
                <div class="dash-card">
                    <div class="dash-card-header">
                        <div>
                            <h2 class="dash-card-title">Modules</h2>
                            <div class="dash-card-sub">Click to configure · toggle reflects saved state</div>
                        </div>
                        <span class="dash-pill"><span style="width:6px;height:6px;border-radius:50%;background:var(--good);display:inline-block;"></span> live</span>
                    </div>
                    <div class="dash-module-list">
                        <button type="button" class="dash-module <%= moduleOn.welcome ? 'on' : '' %>" data-tab-link="welcome">
                            <div class="dash-module-icon">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 16c2 0 2-3 4-3s2 3 4 3 2-3 4-3 2 3 4 3"/><path d="M3 11c2 0 2-3 4-3s2 3 4 3 2-3 4-3 2 3 4 3"/></svg>
                            </div>
                            <div class="dash-module-body"><h4>Welcome</h4><p>Cards &amp; farewell</p></div>
                            <div class="dash-module-toggle" aria-hidden="true"></div>
                            <span class="sr-only"><%= moduleOn.welcome ? '— enabled' : '— disabled' %></span>
                        </button>
                        <button type="button" class="dash-module <%= moduleOn.moderation ? 'on' : '' %>" data-tab-link="moderation">
                            <div class="dash-module-icon">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3l7 2.6v5.4c0 4.5-3 7.5-7 8.5-4-1-7-4-7-8.5V5.6L12 3z"/><path d="M9.1 13.7l2.4-2.9M10.8 14.7l2.4-2.9M12.5 15.6l2.4-2.9"/></svg>
                            </div>
                            <div class="dash-module-body"><h4>Moderation</h4><p>Auto-mod, mute, logging</p></div>
                            <div class="dash-module-toggle" aria-hidden="true"></div>
                            <span class="sr-only"><%= moduleOn.moderation ? '— enabled' : '— disabled' %></span>
                        </button>
                        <button type="button" class="dash-module <%= moduleOn.leveling ? 'on' : '' %>" data-tab-link="leveling">
                            <div class="dash-module-icon">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4" y="13" width="3.4" height="7" rx="1.2"/><rect x="10.3" y="9" width="3.4" height="11" rx="1.2"/><rect x="16.6" y="5" width="3.4" height="15" rx="1.2"/></svg>
                            </div>
                            <div class="dash-module-body"><h4>Leveling</h4><p>XP &amp; rank cards</p></div>
                            <div class="dash-module-toggle" aria-hidden="true"></div>
                            <span class="sr-only"><%= moduleOn.leveling ? '— enabled' : '— disabled' %></span>
                        </button>
                        <button type="button" class="dash-module <%= moduleOn.economy ? 'on' : '' %>" data-tab-link="economy">
                            <div class="dash-module-icon">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="8"/><ellipse cx="12" cy="13.3" rx="2.2" ry="1.7" fill="currentColor" stroke="none"/><circle cx="9.3" cy="10.7" r="1" fill="currentColor" stroke="none"/><circle cx="12" cy="9.6" r="1" fill="currentColor" stroke="none"/><circle cx="14.7" cy="10.7" r="1" fill="currentColor" stroke="none"/></svg>
                            </div>
                            <div class="dash-module-body"><h4>Economy</h4><p>Currency · Crowns</p></div>
                            <div class="dash-module-toggle" aria-hidden="true"></div>
                            <span class="sr-only"><%= moduleOn.economy ? '— enabled' : '— disabled' %></span>
                        </button>
                        <button type="button" class="dash-module <%= moduleOn.ai ? 'on' : '' %>" data-tab-link="ai">
                            <div class="dash-module-icon">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7.4 7.3L6.1 4l3.3 1.8M16.6 7.3L17.9 4l-3.3 1.8"/><path d="M5 7.2h14a1.2 1.2 0 0 1 1.2 1.2v6.3a1.2 1.2 0 0 1-1.2 1.2h-6.3L9 20v-3.1H5a1.2 1.2 0 0 1-1.2-1.2V8.4A1.2 1.2 0 0 1 5 7.2z"/><circle cx="9.7" cy="11.6" r=".95" fill="currentColor" stroke="none"/><circle cx="14.3" cy="11.6" r=".95" fill="currentColor" stroke="none"/></svg>
                            </div>
                            <div class="dash-module-body"><h4>Ask Clawdia</h4><p>Chat with Clawdia</p></div>
                            <div class="dash-module-toggle" aria-hidden="true"></div>
                            <span class="sr-only"><%= moduleOn.ai ? '— enabled' : '— disabled' %></span>
                        </button>
                        <button type="button" class="dash-module <%= moduleOn.rss ? 'on' : '' %>" data-tab-link="rss">
                            <div class="dash-module-icon">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 4a16 16 0 0 1 16 16"/><path d="M4 11a9 9 0 0 1 9 9"/><circle cx="5" cy="19" r="1.4" fill="currentColor"/></svg>
                            </div>
                            <div class="dash-module-body"><h4>Daily Digest</h4><p>RSS &amp; news feeds</p></div>
                            <div class="dash-module-toggle" aria-hidden="true"></div>
                            <span class="sr-only"><%= moduleOn.rss ? '— enabled' : '— disabled' %></span>
                        </button>
                        <button type="button" class="dash-module" data-tab-link="analytics">
                            <div class="dash-module-icon">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 3v18h18"/><path d="M7 14l4-4 3 3 5-6"/></svg>
                            </div>
                            <div class="dash-module-body"><h4>Analytics</h4><p>Insights &amp; event log</p></div>
                        </button>
                    </div>
                </div>

                <!-- Ask Clawdia card -->
                <div class="dash-card dash-bot-card">
                    <div class="dash-card-header">
                        <div>
                            <h2 class="dash-card-title">Ask Clawdia</h2>
                            <div class="dash-card-sub">AI mod assistant</div>
                        </div>
                        <%- include('../brand-mark', { size: 28, fill: '#fff', accent: '#14110d' }) %>
                    </div>
                    <div class="dash-bot-content">
                        <div class="dash-bot-avatar">🐱</div>
                        <div class="dash-bot-text" id="clawdia-msg">
                            <span style="opacity:.5;font-size:.85em">Checking server health…</span>
                        </div>
                    </div>
                    <div id="clawdia-actions" style="display:flex;flex-wrap:wrap;gap:.5rem;margin-top:.5rem"></div>
                </div>
            </div>

            <!-- Recent Activity feed -->
            <div class="dash-card" style="margin-top:1.25rem" id="overview-activity-card">
                <div class="dash-card-header">
                    <div>
                        <h2 class="dash-card-title">Recent Activity</h2>
                        <div class="dash-card-sub">Latest signals from your server</div>
                    </div>
                    <span class="dash-pill" style="font-size:10px;opacity:.6" id="overview-last-updated"></span>
                </div>
                <div id="overview-activity-feed" aria-live="polite" style="display:flex;flex-direction:column;gap:.5rem;min-height:60px">
                    <div id="activity-feed-skeleton" style="width:100%">
                        <div class="skel-bar" style="width:80%"></div>
                        <div class="skel-bar" style="width:65%"></div>
                        <div class="skel-bar" style="width:75%"></div>
                    </div>
                </div>
            </div>

        </section><!-- /#overview -->
